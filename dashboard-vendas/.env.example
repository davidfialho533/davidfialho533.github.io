MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=sua_senha
MYSQL_DATABASE=projeto_login

# Opcional: substitua a query padrão por uma adaptada ao seu banco.
# A query deve retornar pelo menos as colunas: data, valor
# Colunas opcionais: produto, categoria, quantidade, cliente
#
# SALES_QUERY=SELECT data_venda AS data, valor_total AS valor, produto, categoria FROM minha_view
